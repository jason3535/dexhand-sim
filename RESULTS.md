# 第 1 步实验结果 · 抓住到底依赖什么

一句话：**依赖手指有没有把物体包过去（几何包络），不依赖摩擦，也不看接触点数量。**

> 全部数字来自 `sweep_output.txt`（`./.venv/bin/python sweep.py` 一次跑完，约 55s），
> 明细在 `results.csv` / `calibrate.csv`。每个条件都在 5 个毫米级扰动的落点上重复跑，
> 表里给的是**中位数**，方括号是 5 次的最小–最大。

---

## 0. 先纠一个错：原来的 baseline 是假的

原 `grasp.py` 把方块写死在 `pos="0.03 0.02 0.10"`。这只手的掌面在 **z≈0.129–0.135**，
也就是说方块的初始位置在**手掌内部**，一开始就穿模 6mm：

```
t=0.00  方块与手的接触点 46 个，法向力 96 万 N，穿透深度 -6.00mm
```

那份 README 里写的「28 个接触点，方块未落地 ✋」不是抓住了，
是穿透回弹力把它顶住了。所有力的量级（十万 N 级）也都是这么来的。

修好之后（`sim.rest_height()` 二分找出「刚好不碰到张开的手」的高度，贴着掌面放）：

```
t=0.00  接触 0 点
t=2.40  接触 3 点，单点法向力 8–18N，穿透深度 -0.09mm
```

**教训：仿真里第一件要检查的事，是初始状态有没有穿模。**
一个穿模的场景什么结论都能给你，而且看起来完全正常。

## 1. 评分口径：不看「掉没掉」，看「拽不拽得走」

掌心朝上时，没抓住的东西也会安安静静躺在掌心。所以「方块未落地」根本不是判据。

改成：抓稳后**沿掌面法向往上拽**，力从 0 匀速加到 60N（10 N/s），
记录物体被拽走 3cm 时的力 —— 这就是**抓握强度**，用「多少倍物体自重」表示。

> **≥ 1× 自重 = 把手翻过来它也掉不下去 = 真的抓住了，不是托着。**

---

## 2. 落点：这只手的「甜区」只有一条，宽约 2cm

`calibrate.py` 把基准方块在掌心上扫了 7×7 个位置（单位 cm，数字=抓握强度倍数）：

```
      y=  -4    -2     0     2     4     6     8
x= -4     掉  16.0   3.8   3.6   3.2   2.9   2.9
x= -2     掉  11.4   3.8   8.7   3.5   4.4   3.7
x=  0     掉  11.0   5.5  13.1   3.4   2.9   2.9
x=  2     掉  23.2   4.5   5.5   3.6   3.0   3.0
x=  4     掉  29.4   8.7  39.8  28.8  52.0  30.4   ← 甜区整行
x=  6     掉  12.5   4.2   3.5   3.3   2.9   2.9
x=  8     掉  38.2   7.0   7.4   3.7   4.3   3.5
      (x 越大越靠指尖，y 越负越靠拇指；「掉」= 掉出掌外)
```

两条结构：

- **x≈4cm 整行**（中节指骨正好能压到物体顶上）比别处高一个数量级；
- **y=-2cm 整列**（拇指对指线）在任何 x 上都不差。

而 x 方向只要偏 2cm，40× 就掉回 4×。**落点误差 2cm 就是抓牢和抓不牢的区别。**
后面所有实验固定用 x=0.04 / y=0.02。

## 3. A · 换东西抓（位形、摩擦都不变）

| 物体 | 抓握强度（中位数） | 区间 |
|---|---|---|
| 球 R2.2cm | **44.0×** | 28.1–46.6 |
| 方块 6.5cm | 5.4× | 4.1–6.1 |
| 方块 5.5cm | 5.2× | 4.6–7.3 |
| 球 R3.0cm | 4.6× | 3.1–4.8 |
| 圆柱 立 | 4.3× | 4.1–12.1 |
| 方块 4.4cm | 4.1× | 2.9–39.8 |
| 方块 3.0cm | 3.3× | 3.0–3.5 |
| 圆柱 横 | 3.0× | 2.9–4.9 |
| 细杆 横 | 3.0× | 2.9–3.1 |
| 椭球 | **掉了 5/5** | — |

- 一组固定的关节角只对**特定尺寸**有效：R2.2cm 的球正好嵌进指笼（44×），
  半径加大到 3.0cm 就只剩 4.6×。**「抓得住」是位形和物体尺寸的配对，不是手的属性。**
- 椭球 5 次全掉：它会在掌心滚，等指头合拢时已经滚到笼子外面了。
  → 真机上「先看得见物体在哪、再动手指」不是锦上添花，是必需品。
- 细长杆 3.0×：这种东西这只手基本只能压着，不能真握。

## 4. B/B2/B3 · 摩擦几乎不管用（但有个前提）

**B（基准位形，往上拔）**：μ 从 0.05 扫到 3.0，抓握强度全程在 3.9–5.7× 之间，
只有 μ=3.0 略升到 9.9×。**基本是平的。**

**B2（先夹紧到 ×1.5，还是往上拔）**：全程 56–80×，**也是平的**。
夹紧之后手指从物体顶上包过去了，这是**形封闭**——往上拔是在跟几何较劲，摩擦插不上手。

**B3（同样夹紧 ×1.5，改成沿掌面横推 +y）**：这才轮到摩擦出场：

| μ | 0.05 | 0.1 | 0.2 | 0.3 | 0.5 | 0.8 | 1.2 | 1.6 | 2.0 | 3.0 |
|---|---|---|---|---|---|---|---|---|---|---|
| 横推强度 | 46.7× | 46.7× | 46.7× | 47.2× | 45.9× | 54.9× | 69.0× | 77.8× | 74.7× | 90.4× |

μ 从 0.5 涨到 3.0，横推强度差不多翻倍。

⚠️ **注意 μ≤0.2 三行数字一模一样，这不是巧合，是 MuJoCo 的坑：**
接触对的摩擦取两个 geom 的**逐元素最大值**，不是乘积也不是平均。
LEAP 手自己的碰撞体是 `friction=0.2`、指尖是 `0.5`，实测：

```
物体 friction=0.05 -> 实际接触对摩擦 [0.2, 0.5]
物体 friction=0.2  -> 实际接触对摩擦 [0.2, 0.5]
物体 friction=0.8  -> 实际接触对摩擦 [0.8]
```

**你把物体调得再滑都没用，得同时改手的 geom。** 想模拟「抓一块湿肥皂」，
只改物体的 friction 是无效实验。

## 5. C · 闭合幅度才是主变量

基准位形整体 × 一个系数，其它都不动：

| 系数 | ×0.3 | ×0.5 | ×0.7 | ×0.85 | ×1.0 | ×1.15 | ×1.3 | ×1.5 | ×1.8 |
|---|---|---|---|---|---|---|---|---|---|
| 抓握强度 | 2.9× | 2.9× | 3.0× | 2.9× | 4.1× | 19.9× | **41.9×** | **66.7×** | **115.8×** |

单调、跨度 40 倍，是所有变量里最干净的一条曲线。而且有个明显的**阈值**：
×1.0 → ×1.15 之间，从 4× 跳到 20×。那一下就是手指**从"贴着"变成"包过去"**的瞬间。

原来的基准位形 `BASE_POSE`（也就是 ×1.0）正好卡在阈值下沿 —— 抓得住，但很虚。
`grasp.py` 现在默认用 ×1.3。

## 6. D · 16 个关节里，真正在出力的只有 6 个

每个关节单独 ±0.3rad，看抓握强度怎么变（基准 4.1×）：

| 关节 | −0.3 | +0.3 | 落差 |
|---|---|---|---|
| **mf_mcp**（中指根） | 3.5× | **27.7×** | **24.2** |
| **th_cmc**（拇指腕掌） | 3.5× | 11.0× | 7.5 |
| **mf_pip**（中指近节） | 3.6× | 10.5× | 6.9 |
| **th_axl**（拇指轴向） | 3.8× | 10.4× | 6.6 |
| **th_mcp**（拇指掌指） | 3.1× | 8.9× | 5.8 |
| **mf_dip**（中指远节） | 3.5× | 8.0× | 4.5 |
| if_mcp / if_rot / if_dip / th_ipl | — | — | 1.2–2.7 |
| rf_*（无名指 4 个）/ mf_rot / if_pip | — | — | **≈0.0** |

- **无名指 4 个关节完全没参与**（落差 0.1）：在这个落点上它压根碰不到物体。
  16 个自由度，真正在这次抓取里贡献的是 6 个。
- 所有敏感关节都是 **+0.3（更弯）→ 更牢**，和第 5 节完全一致。
- 中指根 `mf_mcp` 一个关节就能把 4× 变成 28×。

## 7. 接触点数量 ≠ 抓得牢

跨 29 个条件算相关性：

```
接触点数 vs 抓握强度   r = +0.25    ← 基本没关系
法向力   vs 抓握强度   r = +0.91    ← 强相关
```

细杆 6 个接触点只有 3.0×，球 3 个接触点却有 44×。
**「接触点多」只说明贴上了，说明不了握住了。** 真机上同理：
铺一堆触觉传感器读到"到处都有接触"，不等于抓稳了 —— 得读**法向力**。

---

## 8. 附：手势与拇指对指（`gestures.py`）

抓取之外顺手试了「摆姿势」——18 个手势，出 `gestures.png`（拼图）和
`gestures.mp4`（31s 动画），viewer 里也能用关键帧逐个切。

**先说一句结构性的：这只手只有 3 根手指 + 拇指，没有小指。** 所以「数到 5」做不了，
只能数到 4；`张开五指` 其实是四指张开。这在挑硬件时是个容易被忽略的事实。

### 8.1 拇指够得到每一根手指

「捏」这类手势没有手写角度，是解出来的：在该指的 4 个 + 拇指的 4 个关节上，
用 `mj_forward` 做纯运动学的坐标下降 + 随机重启，把两个指尖拉到一起，
再跑一遍动力学看**指尖 geom 是不是真的产生了接触**。

| 拇指去够 | 运动学可达 | 落实到动力学后指尖中心距 | 真接触？ |
|---|---|---|---|
| 食指 | 18.0mm | 26.1mm | ✅ |
| 中指 | 18.0mm | 24.6mm | ✅ |
| 无名指 | 18.0mm | 23.7mm | ✅ |

三根全部够得到 —— 这只 16 DoF 的手在**对指（opposition）**这项上是完整的，
不像很多低成本手的拇指只能对到食指。这是它值得当第一只手的一个理由。

> 小坑：指尖 geom 是**长方体**（半边长约 11×12×17mm），不是球，
> 没有干净的「半径」可用。所以判据只能是「有没有真的产生接触」，
> 不能用中心距减半径。

### 8.2 开环下发角度，手到不了那个位置

这是这一节最有用的发现。把解出来的角度直接当指令下发，稳态误差：

```
握拳 13.9°   指点 8.2°   抓握 7.6°   点赞 2.3°
```

第一反应是「kp=3.0 太软」。但把 kp 从 3 一路加到 120：

| kp | 3 | 8 | 20 | 50 | 120 |
|---|---|---|---|---|---|
| 最大角误差 | 6.0° | 5.6° | 5.7° | 4.4° | 5.5° |

**几乎不动。** 说明这 6° 不是伺服软，是**手指之间自碰撞**顶住了 ——
指令再硬也进不去，因为那个位形物理上就不存在。

解决办法是迭代补偿：`cmd += gain × (目标 − 实测)`，跑几轮取最好的一次。
增益取 1.0 会来回震荡（自碰撞让误差非线性），**0.6 才收敛**。

> 对真机的意义：**开环 replay 一组关节角是不可靠的**。
> 从仿真里学到的策略、从遥操作录下来的轨迹，下发到真手上都会有这种量级的偏差，
> 而且 P 增益调高压不掉它。这是第 2 步做手内旋转、第 3 步做 sim2real 时的直接麻烦。

---

## 三条能带走的直觉

1. **抓握 = 几何包络 + 法向力，摩擦是二阶项。**
   往上拔的方向靠包络（摩擦完全没用），沿掌面推的方向才靠摩擦（μ 翻 6 倍 → 强度翻 1 倍）。
   看硬件参数表时：**关节行程和指长比例**决定包不包得住，比指面材料摩擦系数重要得多。

2. **一组固定关节角只服务一种物体尺寸。**
   球 R2.2 是 44×、R3.0 是 4.6×、椭球直接掉。
   这就是为什么灵巧手离不开感知和闭环 —— 开环 replay 一组角度，换个东西就废。

3. **这个系统对毫米级差异是混沌的。**
   同一条件下 5 次微扰动，结果能从 2.9× 到 39.8×。
   任何"跑一次就下结论"的仿真实验都不可信，**必须重复 + 报区间**。
   这条对后面上 RL 更要命：reward 曲线的方差主要来自这里，不是来自算法。

## 局限（下次要补的）

- 只测了 **+z 拔** 和 **+y 推** 两个方向，没测力矩（拧）。真实失败模式多是"转出去"。
- 手是**固定在世界坐标系**的，没有手腕/手臂，也没有"抓起来搬走"这一步。
- 物体质量固定 50g。质量扫描没做 —— 但抓握强度已经用"倍自重"归一化了，影响有限。
- 力的绝对值（几十 N）反映的是这个 MuJoCo 模型的 `kp=3.0` 位置伺服 + 很硬的
  `solref=0.0001` 接触设定，**不要当成真机 LEAP Hand 的出力**。跨条件的相对比较才有效。
- 所有结论都建立在**掌心朝上、物体自己躺进去**的静态起手式上，不含接近轨迹。
